# Para la búsqueda automática de hiperparámetros.
optuna>=4.0,<5.0
# Embeddings multilingües para el RAG semántico híbrido.
sentence-transformers>=3.0
# UI local y asistente generativo de texto basado en Gemini.
streamlit>=1.40
google-genai>=1.0
python-dotenv>=1.0
# Evaluación de máscaras binarias del Componente 2.
Pillow>=10.0
numpy>=1.26
# Generador local de referencias sintéticas para C2 (usa GPU CUDA).
diffusers>=0.31
accelerate>=1.0
safetensors>=0.4
# Backend de Stable Diffusion. Para aprovechar CUDA, instalar la build CUDA de
# PyTorch indicada en el README dentro del mismo entorno virtual.
torch>=2.4
